[diff]
  pager = "delta"

[data]
  # Optional components (prompted during init/apply)
  advancedInstall = {{ promptBool "Install advanced suite of tools (fish, helix, zellij, lima)" }}
  cloudInstall = {{ promptBool "Install CloudOps tools (kubectl + k9s, minikube, terraform, ansible)" }}
  opavailable = {{ promptBool "Do you have a 1Password account (Otherwise, gopass and Secretive will be installed and used)" }}
  
[hooks.read-source-state.pre]
    command = ".local/share/chezmoi/.install-pre-init.sh"