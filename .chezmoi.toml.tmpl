[diff]
  pager = "delta"

[data]
  # Optional components (prompted during init/apply)
  caskInstall   = {{ promptBool "Install GUI apps & fonts via Homebrew Casks" }}
  langInstall   = {{ promptBool "Install programming language tools (Node via fnm, Go, LSPs)" }}
  devopsInstall = {{ promptBool "Install DevOps tools (kubectl, minikube, terraform, ansible)" }}