[diff]
  pager = "delta"

[data]
  # Optional components (prompted during init/apply)
  minimalInstall = {{ promptBool "Install only minimal set of tools (zsh, nvim, fzf and friends)" }}
  devopsInstall = {{ promptBool "Install DevOps tools (kubectl + k9s, minikube, terraform, ansible)" }}
  
[hooks.read-source-state.pre]
    command = ".local/share/chezmoi/.install-pre-init.sh"